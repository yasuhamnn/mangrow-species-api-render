# Render FREE Python buildpack deps.
# Render supports Python 3.11 buildpack. We explicitly pin CPU-only PyTorch so we don't
# pull the ~2.4GB CUDA wheel (that fails on Render's 512MB free tier build memory).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
torchvision==0.19.1

timm==1.0.11
numpy==1.26.4
pillow==10.4.0
fastapi==0.115.6
uvicorn[standard]==0.32.0
python-multipart==0.0.12
